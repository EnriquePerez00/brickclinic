# Computer Vision Module Dependencies
# Install with: pip install -r requirements_cv.txt

# Core CV libraries
opencv-python-headless>=4.8.0
scikit-image>=0.21.0
scikit-learn>=1.3.0
Pillow>=10.0.0
numpy>=1.24.0

# SAHI for sliced inference
sahi>=0.11.0

# YOLO for object detection
ultralytics>=8.0.0

# FAISS for vector search (CPU version)
faiss-cpu>=1.7.4
# For GPU: use faiss-gpu instead

# Deep learning
torch>=2.0.0
torchvision>=0.15.0

# FastAPI ecosystem (if not already installed)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Pydantic for data validation
pydantic>=2.0.0

# Optional: BlenderProc for synthetic data generation
# blenderproc>=2.6.0  # Install separately, requires Blender

# Optional: Metrics and visualization
# matplotlib>=3.7.0
# seaborn>=0.12.0
